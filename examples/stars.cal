include "cores/select.cal"
include "std/io.cal"

let cell x
let cell y

while y 20 < do
	while x 40 < do
		if x 1 + y 1 + % 0 = then
			'*' print_ch
		else
			' ' print_ch
		end

		x 1 + -> x
	end
	new_line
	y 1 + -> y
	0 -> x
end
